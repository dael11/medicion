# En el build de produccion el backend sirve la app en el mismo dominio,
# por eso la API es una ruta relativa.
REACT_APP_API_URL=/api
# El lint no es necesario para generar el bundle:
DISABLE_ESLINT_PLUGIN=true
GENERATE_SOURCEMAP=false
